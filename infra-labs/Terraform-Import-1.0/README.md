First example : Took an existing resource ec2 which was already running on an console and then after that used terraform init and terraform plan and then terraform import aws_isntance.name 'ami-id'

Doing this it allowed it me to understand and match the local and the desired state, hence it updated statefile with all the informations 

